{
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npx ts-node src/index.ts",
    "test": "jest --coverage",
    "test:unit": "jest tests/unit --coverage=false",
    "test:integration": "jest tests/integration --coverage=false",
    "test:e2e": "jest tests/e2e --coverage=false",
    "lint": "npx eslint src/**/*.ts tests/**/*.ts || exit 0",
    "lint:fix": "npx eslint src/**/*.ts tests/**/*.ts --fix"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.10.1",
    "@types/pg": "^8.15.6",
    "@types/supertest": "^6.0.3",
    "jest": "^30.2.0",
    "supertest": "^7.1.4",
    "ts-jest": "^29.4.5",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
